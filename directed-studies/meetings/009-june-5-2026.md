# CPS Meeting

### Planning Notes

* Can move the robot using joints
* Can move robots using IK
  * One of the solvers was resulting in the robot folding in on itself so I 
* Using the RABIT config
  * Created a script and wired it into the workflow that does the translation between the json files
* Static objects
  * Adjusted the motion planner

### Meeting Notes

* Zainab will double check the rabit configuration
* Use existing techniques to get the shape that we want. We shouldn't be doing this manually.
* IvoryOS thinks that collaboration is an option, but this means that we would add the safety mechanism as part of the IvoryOS workflow
* We will set up a meeting with Ivory to further discuss the option for collaboration
  * She may come to the next Friday meeting
* RABIT (decoupling RABIT + RATracer)
  * GRPC is a potential method, but may not be the best method
  * RATracer is the only part that's connected to the device
  * How can we start a process at a random time (without restarting the server)?
    * RATracer is a server so when you start RABIT at a random point in time, try to register with the server
  * Zainab will create a step-by-step manual for an experiment execution to understand the system
  * GPRC allows you to put RABIT somewhere else

### Actionable Items
* Should try adding getting the objects themselves in the environment (not the basic objects)
* Should try doing a 3D scan of objects and add this to the environment
* Should try adding different vendors of robots to the environment (adding additional support for other robots)