### Planning Notes
- Recap of the summer:
    - Lots of trial and error → non-linear
        - 12 detailed progress reports which go into more week-by-week detail
    - Spent some time creating a concrete plan
    - Tried configuring gripper → configuration difficulty
    - Tried preconfigured arm + gripper → couldn’t spawn multiple robots and experienced configuration difficulty
    - Out-of-the-box [ROS2 PACKAGES] didn’t work
        - May be fixed now → unresolved github issues regarding this
- Current state of the project:
    - Collision avoidance:
        - Automated scripts that bring up an environment with MoveIt configured robots based on the coordinates entered by the user
            - Can plan and execute movements
        - Why are scripts used for this? → moveit setup assistant should not be used by the end user to configure the environment
    - Collision detection:
        - I also set up tests for collision detection
            - This uses overlapping joint states
    - I augmented Iris’ current documentation
        - Using sphinx
    - Created scripts
        - run automation scripts
        - run integration tests
- What do we need to do next?
    - Create new plan with concrete timeline and tangible tasks
    - Update environment set up script
    - Hein lab doesn’t use ROS 2 but rather scripts for each individual robot/apparatus
        - Miti (from Eng Phys) noted that this would likely mean we would need to switch Hein Lab over to ROS 2 → this task would have a very large scope
    - 3D model via polycam is out of scope → this creates one large object
    - Next tangible task → place objects in the brought up environment and essentially construct an environment “from hand”
        - How can we do this efficiently
    - Add more tests for collision detection → make the test suite more comprehensive

### Actionable Items
- Test with 3 robots