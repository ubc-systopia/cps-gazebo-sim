# Notes:

### Create Script to use MoveIt to move robots rather than doing this with RViz

- **Getting the first part of the demo working — joint-space control:**
  - Chose MoveIt (planning + IK) over publishing straight to the
    `joint_trajectory_controller` (controller commands with no planning or
    collision-checking), so we could later command poses too, not just joints.
  - Intended to use `moveit_py`, but it wasn't installed — switched to driving
    the running `move_group` via its `MoveGroup` action interface instead.
  - Built an `ArmFleet`/`Arm` Python API; later extracted it into a reusable
    `multi_arm_control` library with a thin per-package `workflow_demo.py`.
  - First move looked frozen (slow first plan + no feedback) — added progress
    logging and faster motion.
  - Used deterministic **joint-space goals** (commanding the six joint angles via
    MoveIt): `home` (all-zeros) and `upright`. No IK, so they move predictably and
    won't dive at the floor the way a Cartesian goal can. First reliable motion —
    single-arm, then both arms.
  - Moving two arms at once: tried `move_all` (independent parallel plans, not
    collision-aware) vs `move_coordinated` (one combined collision-checked plan);
    the coordinated approach worked.
- **Adding IK / Cartesian control:**
  - Added `move_pose` for end-effector pose goals — the original "enter the
    position of the end of the arm" requirement; `move_group` runs IK to turn the
    pose into joint angles.
  - Simultaneous Cartesian motion (`move_poses`, plan-only path) failed with
    `INVALID_MOTION_PLAN` — replaced with `move_coordinated_poses` (each pose →
    joints via `/compute_ik` → coordinated plan).
  - "Pretzel" folding (arms knotting up until stuck) traced to the IK solver used
    previously, **KDL** (`kdl_kinematics_plugin/KDLKinematicsPlugin`, the default
    in the generated `kinematics.yaml`); wanted TRAC-IK (unavailable in apt),
    installed **`pick_ik`** instead.
  - Switching to `pick_ik` fixed it — full test pass in sim across all modes
    (single/multi-arm, joint/Cartesian, sequential/coordinated). Remaining: slow
    planning (~12–24 s/move).
- **Create API layer that can be called from scripts**

### Understanding the interbotix repo
* Uses LMA (Leveberg-Marquadt Algorithm) for ViperX
  * LMA is an iterative numerical IK solver — it starts from the current joint
  configuration and uses gradient descent (with the Levenberg-Marquardt method) to
  iteratively minimize the error between the current end-effector pose and the target
  pose.
  * Comparing pick_ik with LMA and KDL
    * pick_ik vs LMA:
      - LMA is pure gradient descent — it can get stuck in local minima and fail to find a
      solution if the initial joint configuration is far from the target
      - pick_ik adds random restarts to escape local minima, making it much more reliable
      for difficult configurations
      - pick_ik supports custom cost functions (e.g., prefer solutions that minimize joint
      movement, avoid certain configurations) — LMA has no equivalent
      - pick_ik tends to find better solutions for redundant joints

    * pick_ik vs KDL:
      - KDL is the oldest and weakest of the three — slow, frequent failures near
      singularities
      - pick_ik significantly outperforms KDL on both speed and success rate
* NED2's IK solver is configured on the robot's onboard computer, not in the repo
  * Niryo's default ROS stack for the NED2 uses KDL


### Refactor ROS package generation scripts
* Create a demo script that controls the robot using joint angles and IK
* Refactor ROS package generation so that all the configs are in a json file.
  * The config file can be used to write from scratch or can be generated by first entering the configs into the CLI when prompted then regenerating the package
  * RViz can be toggled on and off

### Challenges
* There's a noticeable lag for the motion planning - performance issue

# Status Report:

# Feedback

### Unfinished content (blanks / cut-off sentences)
- **[Overall Summary]** Sentence ends in `______` — needs a one-line summary of the two weeks' work.
- **[Adding IK/Cartesian Control]** "When planning some simple poses and " cuts off mid-sentence — right before the most important result (see IK section below).
- **[Understanding the interbotix repo]** Marked only `TODO` — fill in, or state explicitly "in progress / not covered this period" so a reader can tell deferral from omission.
- **[Refactor ROS package generation scripts]** Same — bare `TODO`; clarify status.
- **[Use of GenAI]** Ends mid-sentence at "to assist with developing " — complete the thought.

### Missing headline result (highest priority)
- **[Adding IK/Cartesian Control]** The report introduces KDL but stops before the actual finding: KDL produced wound-up "pretzel" solutions for 6-DOF orientation goals → TRAC-IK was wanted but unavailable in apt → installed `pick_ik` instead → fixed it, with a full test pass across all modes. This diagnose→evaluate→switch arc is the strongest engineering result of the two weeks and is currently absent.

### Technical accuracy / strengthening
- **[Current Challenges]** Replace "noticeable lag" with the concrete figure (~12–24 s per move) — quantified problems read as real observations.
- **[Joint-space control]** The "motion plans are based on the static configuration at planning time" explanation is accurate and well-put; name the two approaches compared (independent parallel plans vs. one combined collision-checked plan) to make it concrete.
- **[Create Script to use MoveIt…]** Worth noting the API drives the running `move_group` via its action interface because `moveit_py` wasn't installed — explains a real constraint that was worked around.

### Consistency / wording
- **[Overall Summary / Breakdown of Last Two Weeks]** "third week back" vs "Last Two Weeks" timeframe mismatch — reconcile.
- **[Current Challenges]** "heedlessly" → "headlessly" (real technical term; the headless-vs-GUI production point is a good one).
- **[Adding IK/Cartesian Control]** "Inverse kinematic allows" → "Inverse kinematics allows".
- **[Create API layer…]** "API layer than can be called" → "that can be called".

### Working well (keep)
- **[Breakdown of Last Two Weeks]** Strong motivational narrative — each step is justified (RViz → joint-space → IK) rather than just listed.
- **[Adding IK/Cartesian Control]** "joint angles don't translate to real-world use, IK does" is exactly the right justification for the Cartesian work.
- **[Next Steps]** Concrete and correctly ordered (RABIT config → static objects); matches where the project actually is.